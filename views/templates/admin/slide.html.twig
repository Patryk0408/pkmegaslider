{% extends '@PrestaShop/Admin/layout.html.twig' %}

{# {% form_theme form '@PrestaShop/Admin/TwigTemplateForm/prestashop_ui_kit_base.html.twig' %} #}

{% block content %}
{{ form_start(form) }}
    <div class="row justify-content-center">
      <div class="col-xl-10">
        <div class="card">
          <h3 class="card-header">
            <i class="material-icons">edit</i> {{ 'Nowy slide'|trans({}, 'Modules.PKMegaSlider.Admin') }}
          </h3>

          <div class="card-body">
            <div class="form-wrapper">
              {{ form_widget(form) }}
            </div>
          </div>

          <div class="card-footer">
            <div class="d-flex justify-content-between">
              <a href="{{path("blog_list")}}" class="btn btn-default">{{ 'Back'|trans({}, 'Modules.PKMegaSlider.Admin') }}</a>
              <button class="btn btn-primary">{{ 'Save'|trans({}, 'Modules.PKMegaSlider.Admin') }}</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  {{ form_end(form) }}
{% endblock %}